export interface EmailConfig {
  id: string;
  companyName: string;
  companyCode: string;
  email: string;
}

export interface Company {
  id: string;
  name: string;
  code: string;
  icon: string;
}